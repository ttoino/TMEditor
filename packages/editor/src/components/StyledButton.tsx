import { styled } from "@common/theme";

export default styled("button", {
    padding: "0.3em 0.8em",
    fontSize: "1em",
    border: "none",
    borderRadius: "0.3em",
    backgroundColor: "$primary",
    color: "white",
    cursor: "pointer",
});
